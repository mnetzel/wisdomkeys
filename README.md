# Wisdom Keys

Static GitHub Pages site.

## Editing

- `index.html` contains the page shell.
- `styles.css` contains all visual styling.
- `script.js` contains navigation, page content, language labels, and simple hash routing.

After editing, commit and push to `main`. GitHub Actions will publish the site automatically.

Live site: https://mnetzel.github.io/wisdomkeys/
